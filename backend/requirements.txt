# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Settings and validation
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Data processing — primary MVP engine
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
openpyxl>=3.1.2

# Async file I/O
aiofiles>=23.2.1

# Database — proposal storage (Stage 3+)
sqlalchemy>=2.0.0

# PySpark is optional: install separately when running in Spark mode.
# Requires Java 11+ on the host machine.
# To enable: pip install pyspark>=3.5.0
